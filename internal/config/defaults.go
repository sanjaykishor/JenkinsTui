package config

// DefaultServerURL is the default Jenkins server URL
const DefaultServerURL = "http://localhost:8080"

// DefaultRefreshInterval is the default interval for refreshing data (in seconds)
const DefaultRefreshInterval = 30

// DefaultMaxLogLines is the maximum number of log lines to retrieve
const DefaultMaxLogLines = 1000

// DefaultTheme is the default UI theme
const DefaultTheme = "dark"
